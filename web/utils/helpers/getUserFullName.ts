/**
 * Gets the user's full name in string.
 * 
 * @returns The user's full name string.
 */

// TO-DO: make logic

const getUserFullName = () => {
  return "Suankularb Kornor"
}

export default getUserFullName;